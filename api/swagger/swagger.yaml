swagger: '2.0'

info:
  version: "0.0.1"
  title: Spice
  description: Сatalog of astronomical objects.
    
host: localhost:10010
basePath: /
schemes:
  - http

consumes:
  - application/json
produces:
  - application/json
  

## Cross-request parameters

parameters:
  constellationIdParam:
    in: path
    name: constellationId
    description: ID of the constellation to operate with.
    required: true
    type: integer
    format: int64
  starIdParam:
    in: path
    name: starId
    description: ID of the star to operate with.
    required: true
    type: integer
    format: int64
  nebulaIdParam:
    in: path
    name: nebulaId
    description: ID of the nebula to operate with.
    required: true
    type: integer
    format: int64
  galaxyIdParam:
    in: path
    name: galaxyId
    description: ID of the galaxy to operate with.
    required: true
    type: integer
    format: int64
  planetarySystemIdParam:
    in: path
    name: planetarySystemId
    description: ID of the planetary system to operate with.
    required: true
    type: integer
    format: int64
  planetIdParam:
    in: path
    name: planetId
    description: ID of the planet to operate with.
    required: true
    type: integer
    format: int64
  dwarfPlanetIdParam:
    in: path
    name: dwarfPlanetId
    description: ID of the dwarf planet to operate with.
    required: true
    type: integer
    format: int64
  asteroidIdParam:
    in: path
    name: asteroidId
    description: ID of the asteroid to operate with.
    required: true
    type: integer
    format: int64
  cometIdParam:
    in: path
    name: cometId
    description: ID of the comet to operate with.
    required: true
    type: integer
    format: int64
  satelliteIdParam:
    in: path
    name: satelliteId
    description: ID of the satellite to operate with.
    required: true
    type: integer
    format: int64
  
  
## Requests

paths:

  #Celestail Objects
  /objects:
    x-swagger-router-controller: objects
    get:
      operationId: listObjects
      description: |
        Gets all objects available in catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/CelestialObject"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  /planetoids:
    x-swagger-router-controller: objects
    get:
      operationId: listPlanetoids
      description: |
        Gets all planetoids available in catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Planetoid"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  #Constellations
  /objects/constellations:
    x-swagger-router-controller: constellations
    get:
      tags:
        - constellation
      operationId: listConstellations
      description: |
        Gets all constellations from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Constellation"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - constellation
      operationId: createConstellation
      description: |
        Creates a new constellation.
      parameters:
        - in: body
          name: constellation
          description: The constellation to create.
          required: true
          schema:
            $ref: "#/definitions/Constellation"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Constellation"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"            
  /objects/constellations/{constellationId}:
    x-swagger-router-controller: constellations
    parameters:
      - $ref: "#/parameters/constellationIdParam"
    get:
      tags:
        - constellation
      operationId: getConstellation
      description: |
        Gets a constellation with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Constellation"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"

  #Stars            
  /objects/stars:
    x-swagger-router-controller: stars
    get:
      tags:
        - star
      operationId: listStars
      description: |
        Gets all stars from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Star"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/constellations/{constellationId}/stars:
    x-swagger-router-controller: stars
    parameters:
      - $ref: "#/parameters/constellationIdParam"
    get:
      tags:
        - star
      operationId: listConstellationStars
      description: |
        Gets all stars in constellation.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Star"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - star
      operationId: createStar
      description: |
        Creates a new star.
      parameters:
        - in: body
          name: star
          description: The star to create.
          required: true
          schema:
            $ref: "#/definitions/Star"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Star"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                
  /objects/constellations/{constellationId}/stars/{starId}:
    x-swagger-router-controller: stars
    parameters:
      - $ref: "#/parameters/constellationIdParam"
      - $ref: "#/parameters/starIdParam"
    get:
      tags:
        - star
      operationId: getStar
      description: |
        Gets a star with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Star"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  # Nebulas
  /objects/nebulas:
    x-swagger-router-controller: nebulas
    get:
      tags:
        - nebula
      operationId: listNebulas
      description: |
        Gets all nebulas from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Nebula"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/constellations/{constellationId}/nebulas:
    x-swagger-router-controller: nebulas
    parameters:
      - $ref: "#/parameters/constellationIdParam"
    get:
      tags:
        - nebula
      operationId: listConstellationNebulas
      description: |
        Gets all nebulas in constellation.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Nebula"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - nebula
      operationId: createNebula
      description: |
        Creates a new nebula.
      parameters:
        - in: body
          name: nebula
          description: The nebula to create.
          required: true
          schema:
            $ref: "#/definitions/Nebula"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Nebula"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                
  /objects/constellations/{constellationId}/nebulas/{nebulaId}:
    x-swagger-router-controller: nebulas
    parameters:
      - $ref: "#/parameters/constellationIdParam"
      - $ref: "#/parameters/nebulaIdParam"
    get:
      tags:
        - nebula
      operationId: getNebula
      description: |
        Gets a nabula with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Nebula"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  # Galaxies
  /objects/galaxies:
    x-swagger-router-controller: galaxies
    get:
      tags:
        - galaxy
      operationId: listGalaxies
      description: |
        Gets all galaxies from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Galaxy"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - galaxy
      operationId: createGalaxy
      description: |
        Creates a new galaxy.
      parameters:
        - in: body
          name: galaxy
          description: The galaxy to create.
          required: true
          schema:
            $ref: "#/definitions/Galaxy"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Galaxy"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"  
  /objects/galaxies/{galaxyId}:
    x-swagger-router-controller: galaxies
    parameters:
      - $ref: "#/parameters/galaxyIdParam"
    get:
      tags:
        - galaxy
      operationId: getGalaxy
      description: |
        Gets a galaxy with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Galaxy"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  # Planetary systems
  /objects/planetary-systems:
    x-swagger-router-controller: planetary_systems
    get:
      tags:
        - planetary system
      operationId: listPlanetarySystems
      description: |
        Gets all planetary systems from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/PlanetarySystem"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/galaxies/{galaxyId}/planetary-systems:
    x-swagger-router-controller: planetary_systems
    parameters:
      - $ref: "#/parameters/galaxyIdParam"
    get:
      tags:
        - planetary system
      operationId: listGalaxyPlanetarySystems
      description: |
        Gets all planetary systems in galaxy.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/PlanetarySystem"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - planetary system
      operationId: createPlanetarySystem
      description: |
        Creates a new planetary system.
      parameters:
        - in: body
          name: planetarySystem
          description: The planetary system to create.
          required: true
          schema:
            $ref: "#/definitions/PlanetarySystem"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/PlanetarySystem"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"   
  /objects/galaxies/{galaxyId}/planetary-systems/{planetarySystemId}:
    x-swagger-router-controller: planetary_systems
    parameters:
      - $ref: "#/parameters/galaxyIdParam"
      - $ref: "#/parameters/planetarySystemIdParam"
    get:
      tags:
        - planetary system
      operationId: getPlanetarySystem
      description: |
        Gets a planetary system with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/PlanetarySystem"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    
  # Planets
  /objects/planets:
    x-swagger-router-controller: planets
    get:
      tags:
        - planet
      operationId: listPlanets
      description: |
        Gets all planets from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Planet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planetary-systems/{planetarySystemId}/planets:
    x-swagger-router-controller: planets
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
    get:
      tags:
        - planet
      operationId: listPlanetarySystemPlanets
      description: |
        Gets all planets in planetary system.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Planet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - planet
      operationId: createPlanet
      description: |
        Creates a new planet.
      parameters:
        - in: body
          name: planet
          description: The planet to create.
          required: true
          schema:
            $ref: "#/definitions/Planet"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Planet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"    
  /objects/planetary-systems/{planetarySystemId}/planets/{planetId}:
    x-swagger-router-controller: planets
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
      - $ref: "#/parameters/planetIdParam"
    get:
      tags:
        - planet
      operationId: getPlanet
      description: |
        Gets a planet with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Planet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
  # Dwarf planets
  /objects/dwarf-planets:
    x-swagger-router-controller: dwarf_planets
    get:
      tags:
        - dwarf planet
      operationId: listDwarfPlanets
      description: |
        Gets all dwarf planets from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/DwarfPlanet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planetary-systems/{planetarySystemId}/dwarf-planets:
    x-swagger-router-controller: dwarf_planets
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
    get:
      tags:
        - dwarf planet
      operationId: listPlanetarySystemDwarfPlanets
      description: |
        Gets all dwarf planets in planetary system.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/DwarfPlanet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - dwarf planet
      operationId: createDwarfPlanet
      description: |
        Creates a new dwarf planet.
      parameters:
        - in: body
          name: dwarfPlanet
          description: The dwarf planet to create.
          required: true
          schema:
            $ref: "#/definitions/DwarfPlanet"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/DwarfPlanet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"  
  /objects/planetary-systems/{planetarySystemId}/dwarf-planets/{dwarfPlanetId}:
    x-swagger-router-controller: dwarf_planets
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
      - $ref: "#/parameters/dwarfPlanetIdParam"
    get:
      tags:
        - dwarf planet
      operationId: getDwarfPlanet
      description: |
        Gets a dwarf planet with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/DwarfPlanet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
  # Asteroids
  /objects/asteroids:
    x-swagger-router-controller: asteroids
    get:
      tags:
        - asteroid
      operationId: listAsteroids
      description: |
        Gets all asteroids from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Asteroid"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planetary-systems/{planetarySystemId}/asteroids:
    x-swagger-router-controller: asteroids
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
    get:
      tags:
        - asteroid
      operationId: listPlanetarySystemAsteroids
      description: |
        Gets all asteroids in planetary system.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Asteroid"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - asteroid
      operationId: createAsteroid
      description: |
        Creates a new asteroid.
      parameters:
        - in: body
          name: asteroid
          description: The asteroid to create.
          required: true
          schema:
            $ref: "#/definitions/Asteroid"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Asteroid"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /objects/planetary-systems/{planetarySystemId}/asteroids/{asteroidId}:
    x-swagger-router-controller: asteroids
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
      - $ref: "#/parameters/asteroidIdParam"
    get:
      tags:
        - asteroid
      operationId: getAsteroid
      description: |
        Gets a asteroid with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Asteroid"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  # Comets
  /objects/comets:
    x-swagger-router-controller: comets
    get:
      tags:
        - comet
      operationId: listComets
      description: |
        Gets all comets from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Comet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planetary-systems/{planetarySystemId}/comets:
    x-swagger-router-controller: comets
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
    get:
      tags:
        - comet
      operationId: listPlanetarySystemComets
      description: |
        Gets all comets in planetary system.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Comet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - comet
      operationId: createComet
      description: |
        Creates a new comet.
      parameters:
        - in: body
          name: comet
          description: The comet to create.
          required: true
          schema:
            $ref: "#/definitions/Comet"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Comet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planetary-systems/{planetarySystemId}/comets/{cometId}:
    x-swagger-router-controller: comets
    parameters:
      - $ref: "#/parameters/planetarySystemIdParam"
      - $ref: "#/parameters/cometIdParam"
    get:
      tags:
        - comet
      operationId: getComet
      description: |
        Gets a comet with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Comet"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
            
  # Satellites
  /objects/satellites:
    x-swagger-router-controller: satellites
    get:
      tags:
        - satellite
      operationId: listSatellites
      description: |
        Gets all satellites from catalog.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Satellite"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planets/{planetId}/satellites:
    x-swagger-router-controller: satellites
    parameters:
      - $ref: "#/parameters/planetIdParam"
    get:
      tags:
        - satellite
      operationId: listPlanetSatellites
      description: |
        Gets all satellites of planet.
      responses:
        "200":
          description: Success
          schema:
            type: array
            items:
              $ref: "#/definitions/Satellite"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags:
        - satellite
      operationId: createSatellite
      description: |
        Creates a new satellite.
      parameters:
        - in: body
          name: satellite
          description: The satellite to create.
          required: true
          schema:
            $ref: "#/definitions/Satellite"
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Satellite"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /objects/planets/{planetId}/satellites/{satelliteId}:
    x-swagger-router-controller: satellites
    parameters:
      - $ref: "#/parameters/planetIdParam"
      - $ref: "#/parameters/satelliteIdParam"
    get:
      tags:
        - satellite
      operationId: getSatellite
      description: |
        Gets a satellite with a given identifier.
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/Satellite"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
          
  /swagger:
    x-swagger-pipe: swagger_raw


## Data classes

definitions:
  CelestialObject:
    type: object
    properties:
      id:
        type: integer
        description: Unique object identifier.
      name:
        type: string
        description: Object name.
      discoverer:
        type: string
        description: Discoverer of the object.
      discovery_date:
        type: string
        format: date
        description: Discovery date of the object.
      discovery_place:
        type: string
        description: Discovery place of the object.
      age:
        type: number
        format: double
        description: Age of the object.
      volume:
        type: number
        format: double
        description: Volume of the object.
      mass:
        type: number
        format: double
        description: Mass of the object.
      radius:
        type: number
        format: double
        description: Radius of the object.  
      surface_area:
        type: number
        format: double
        description: Surface area of the object.
      mean_density:
        type: number
        format: double
        description: Mean density of the object.  
      whose_satellite:
        type: integer
        description: Identifier of the object around which it rotates.
  Constellation:
    type: object
    allOf:
      - $ref: '#/definitions/CelestialObject' # Constellation has all properties of a CelestialObject
      - properties: # extra properties only for constellation
          limits:
            type: string
            description: Limits of the constellation.
          hill_sphere:
            type: string
            description: Hill sphere of the constellation.
          stars:
            type: array
            items:
              $ref: "#/definitions/Star"
  Star:
    type: object
    allOf:
      - $ref: "#/definitions/CelestialObject"
      - properties:
          right_ascension:
            type: number
            format: double
            description: Right ascension of the star.
          declination:
            type: number
            format: double
            description: Declination of the star.
          apparent_magnitude:
            type: number
            format: double
            description: Apparent magnitude of the star.
          distance:
            type: number
            format: double
            description: Distance from star to Earth.
          radial_velocity:
            type: number
            format: double
            description: Radial velocity of the star.
          luminosity:
            type: number
            format: double
            description: Luminosity of the star.
          effective_temperature:
            type: number
            format: double
            description: Effective temperature of the star.
          constellation_id:
            type: integer
            description: Identifier of the constellation.          
  Nebula:
    type: object
    allOf:
      - $ref: "#/definitions/CelestialObject"
      - properties:
          hill_sphere:
            type: string
            description: Hill sphere of the nebula.
          distance:
            type: number
            format: double
            description: Distance from nebula to Earth.
          snow_line:
            type: number
            format: double
            description: Snow line of the nebula.
          visible_dimensions:
            type: number
            format: double
            description: Visible dimensions of the nebula.
          apparent_magnitude:
            type: number
            format: double
            description: Apparent magnitude of the nebula.
          constellation_id:
            type: integer
            description: Identifier of the constellation.
  Galaxy:
    type: object
    allOf:
      - $ref: "#/definitions/CelestialObject"
      - properties:
          hill_sphere:
            type: string
            description: Hill sphere of the galaxy.
          snow_line:
            type: number
            format: double
            description: Snow line of the galaxy.
          visible_dimensions:
            type: number
            format: double
            description: Visible dimensions of the galaxy.
          surface_brightness:
            type: number
            format: double
            description: Surface brightness of the galaxy.
          distance:
            type: number
            format: double
            description: Distance from galaxy to Earth.
          radial_velocity:
            type: number
            format: double
            description: Radial velocity of the galaxy.
  PlanetarySystem:
    type: object
    allOf:
      - $ref: "#/definitions/CelestialObject"
      - properties:
          hill_sphere:
            type: string
            description: Hill sphere of the planetary system.
          snow_line:
            type: number
            format: double
            description: Snow line of the planetary system.
          galaxy_id:
            type: integer
            description: Identifier of the galaxy.
  Planetoid:
    type: object
    allOf:
      - $ref: "#/definitions/CelestialObject"
      - properties:
          semi_major_axis:
            type: number
            format: double
            description: Semi-major axis of the planetoid.
          aphelion:
            type: number
            format: double
            description: Aphelion of the planetoid.
          perihelion:
            type: number
            format: double
            description: Perihelion of the planetoid.
          eccentricity:
            type: number
            format: double
            description: Eccentricity of the planetoid.
          orbital_inclination:
            type: number
            format: double
            description: Orbital inclination of the planetoid.            
          axial_tilt:
            type: number
            format: double
            description: Axial tilt of the planetoid.
          rotation_period:
            type: number
            format: double
            description: Rotation period of the planetoid.
          temperature:
            type: number
            format: double
            description: Surface temperature of the planetoid.
          planetary_system_id:
            type: integer
            description: Identifier of the planetary system.
  Planet:
    type: object
    allOf:
      - $ref: "#/definitions/Planetoid"
      - properties:
          day:
            type: number
            format: double
            description: Day on the planet.
  DwarfPlanet:
    type: object
    allOf:
      - $ref: "#/definitions/Planetoid"
      - properties:
          mean_anomaly:
            type: number
            format: double
            description: Mean anomaly of the dwarf planet.
  Asteroid:
    type: object
    allOf:
      - $ref: "#/definitions/Planetoid"
      - properties:
          mean_anomaly:
            type: number
            format: double
            description: Mean anomaly of the asteroid.
          snow_line:
            type: number
            format: double
            description: Snow line of the asteroid.
  Comet:
    type: object
    allOf:
      - $ref: "#/definitions/Planetoid"
      - properties:
          epoch:
            type: number
            format: double
            description: Epoch of the comet.
  Satellite:
    type: object
    allOf:
      - $ref: "#/definitions/Planetoid"
          
  ErrorResponse:
    required:
      - message
    properties:
      message:
        type: string
